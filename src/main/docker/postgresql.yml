version: '2'
services:
  verprojects-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/verprojects/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=verprojects
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
